package controllers

import (
	. "github.com/onsi/ginkgo"
)

var _ = Describe("Network Roku access", func() {

	When("Roku access is enabled", func() {

		PIt("exposes Roku ports on the StatefulSet")

		PIt("exposes Roku ports on the headless service")

	})

	When("Roku is enabled with an external service", func() {

		PIt("exposes Roke ports on the external service")

	})
})
